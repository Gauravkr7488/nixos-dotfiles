{
        programs.alacritty = {
                enable = true;
                theme = "gruvbox_dark";
                settings = {
                        window = {
                                decorations = "None";
                                opacity = 0.8;
                        };
                };
        };
}
